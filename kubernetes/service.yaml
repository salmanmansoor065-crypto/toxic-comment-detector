apiVersion: v1
kind: Service
metadata:
  name: toxic-detector-service
spec:
  selector:
    app: toxic-detector
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    nodePort: 30000
  type: NodePort